[build-system]
requires = ["cmeel[build] >=0.56", "roboplan-core ==0.6.1"]
build-backend = "cmeel.build"

[project]
name = "roboplan-hardware-interface"
version = "0.6.1"
description = "Hardware and simulation interface for RoboPlan."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10,<3.15"
authors = [{ name = "Muhammad Ahmed", email = "thisisdeahmed@gmail.com" }]
classifiers = ["License :: OSI Approved :: MIT License"]
dependencies = ["roboplan-core ==0.6.1"]

[project.urls]
Homepage = "https://github.com/open-planning/roboplan"

[tool.pixi.package]
publish = true

[tool.pixi.package.build]
backend = { name = "pixi-build-cmake", version = "*" }

[tool.pixi.package.host-dependencies]
roboplan-core = { path = "../roboplan_core" }

[tool.pixi.package.run-exports.weak]
roboplan-hardware-interface = { pin-subpackage = true }

[tool.cmeel]
run-tests = false
