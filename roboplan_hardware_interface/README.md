# RoboPlan Hardware Interface

This package defines the state and command boundary used to interact with real or simulated robot. Each concrete backend owns the translation from its native representation to the RoboPlan's representation. 