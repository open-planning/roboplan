# Roboplan Python Bindings

This package provides Python bindings to RoboPlan using [nanobind](https://nanobind.readthedocs.io/en/latest/).

The included [CMakeLists.txt](./CMakeLists.txt) supports building either using `colcon` or `scikit-build-core`.
