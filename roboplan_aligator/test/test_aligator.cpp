#include <gtest/gtest.h>

#include <roboplan_aligator/roboplan_aligator.hpp>

namespace roboplan {

// Placeholder test for the package scaffolding (Prompt 1). The real ReducedGroupModel /
// dynamics / cost / constraint / solver-lifecycle tests are added in later prompts per
// the design doc's §8 testing plan.
TEST(RoboPlanAligatorTest, VersionIsNotEmpty) { EXPECT_FALSE(aligatorVersion().empty()); }

}  // namespace roboplan
