@PACKAGE_INIT@

find_package(roboplan REQUIRED)

# aligator is linked PRIVATE and is not part of roboplan_aligator's public interface,
# so consumers do not need to find it here (mirrors roboplan_oink's proxsuite handling).

include("${CMAKE_CURRENT_LIST_DIR}/roboplan_aligatorTargets.cmake")
