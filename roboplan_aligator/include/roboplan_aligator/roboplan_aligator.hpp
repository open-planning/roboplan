#pragma once

#include <string>

namespace roboplan {

/// @brief Returns the roboplan_aligator package version string.
/// @details Placeholder API for the package scaffolding (Prompt 1). The trajectory
/// optimization surface (ReducedGroupModel, TrajOpt* types, cost/constraint factories,
/// TrajectoryOptimizer) is added in later implementation prompts per the design doc.
std::string aligatorVersion();

}  // namespace roboplan
